pre-commit:
  parallel: false
  commands:
    spotless-format:
      glob: "*.{kt,kts}"
      run: ./gradlew spotlessApply
      args: -PspotlessIdeHook={staged_files_absolute}
      stage_fixed: true

    run-tests:
      run: ./gradlew test testDebugUnitTest
